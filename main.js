// Emptying an Array

let numbers = [1, 2, 3, 4, 6, 7, 8, 9];
let another = numbers;

// numbers = [];

numbers.length = 0;

// numbers.splice(0, numbers.length);

console.log(numbers);

console.log(another);
